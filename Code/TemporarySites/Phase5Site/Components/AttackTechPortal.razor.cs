using Microsoft.AspNetCore.Components;
using System;
using System.Linq;
using System.Net.Http;

namespace Phase5Site.Components
{
    public partial class AttackTechPortal
    {
        //when selecting all techs, not sure if the other part would be refreshed.  if somehow it does not, then has to think about what to do.
        //may even need a delegate.  so the view model can invoke and the other part can refresh.

    }
}